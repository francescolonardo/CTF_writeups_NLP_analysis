<h1>Beginners web</h1>

<p>The trick here that we controlled <code>request.body.converter</code>, so we could do <code>__defineSetter__`` on</code>converters` with your sessionID as the first parameter, and the object which would then be resolved as a promise?? The next we set flagConverted on it with our sessionID, the promise gets resolved, some error gets printed and we get the flag. </p>
